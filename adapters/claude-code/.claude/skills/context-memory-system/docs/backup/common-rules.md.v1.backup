# 上下文记忆系统规则

> 🛑 **三秒检查 (每次行动前):**
> 1. 刚检索了吗？→ 累计 ≥3 文件未沉淀？**必须立即沉淀**
> 2. 要创建文件了吗？→ 该更新的更新了吗？
> 3. 用户回复了吗？→ 文档同步了吗？

> 使用持久化 Markdown 文件管理 AI 工作记忆,解决上下文窗口限制和目标漂移问题。

> 📖 **详细示例**: 需要更多说明和示例?请查看 [common-rules-examples.md](.flowmem/docs/common-rules-examples.md)
---

## 核心文件

| 文件 | 定位 | 生命周期 |
|------|------|----------|
| `.agentmem/project.md` | 项目整体描述 | 长期维护 |
| `.agentmem/request.md` | 当前需求澄清 | 任务周期 |
| `.agentmem/todolist.md` | 任务清单(仅列表) | 任务周期 |
| `.agentmem/task_logs/` | 执行日志/总结/问题 | 任务周期 |
| `.agentmem/notes.md` | 研究笔记 | 按需使用 |
| `.agentmem/docs/` | 详细文档目录 | 长期积累 |
| `.agentmem/request_detail/` | 需求对话详情 | 任务周期 |
| `.agentmem/history/` | 历史归档 | 永久保留 |

### 文件详解

| 文件 | 职责 | 关键规则 |
|------|------|----------|
| `project.md` | 项目知识库 | 索引优先，>300行迁移到docs/ |
| `todolist.md` | 任务清单 | 仅 `[ ]` `[/]` `[x]`，详情存 task_logs/ |
| `request.md` | 需求澄清 | >5轮或>150行拆分到 request_detail/ |
| `notes.md` | 研究笔记 | 临时信息，任务结束归档 |
| `task_logs/` | 执行日志 | 命名: `[ID]-[描述].md` |
| `docs/` | 详细文档 | 存放从 project.md 拆分的内容 |

**🔴 project.md vs request.md 边界:**
- 下次任务还有用？→ `project.md`
- 本次需求边界/确认细节？→ `request.md`
---

## 触发判断

**启用信号（满足任一）:**
- 涉及 3+ 文件修改
- 需多轮交流理解意图
- 预计 10+ 次工具调用
- 新功能开发或架构调整
- 用户提到「规划」「分步」「追踪进度」

**跳过信号:**
- 单文件小改/简单问答/快速查询
- 3 次工具调用内可完成

> 💡 **原则**: 宁可多用不可少用。拿不准就先创建 project.md,成本极低。

> 🆕 **冷启动**: 首次使用时创建 `.agentmem/` 目录，至少包含 `project.md`

> 🔄 **会话恢复**: 新对话 → 检查 `.agentmem/` 是否存在
> - 存在且有 `todolist.md` → 按 todolist → request → project 刷新 → 继续任务
> - 存在但无 todolist → 读取 project.md → 等待新需求
> - 不存在 → 正常启动（触发判断）
---

## 🚨 任务进行中禁止退出

**触发:** 已创建 `request.md` 或 `todolist.md`

**核心规则:**
1. 用户的所有回复都是任务的一部分,不要重新判断是否启用
2. 用户要求"先测试""检查一下"等,应在 todolist.md 中记录,而非退出流程
3. 只有用户明确说"停止""取消""不做了",才能归档任务并退出

**场景示例:**
- 用户说"先测试" → 在 todolist.md 中记录 → 执行测试 → 更新状态 → 询问是否继续
- 用户提出明显不相关需求 → 先确认:"这是当前任务的一部分,还是新的独立任务?"

**完成标志:** todolist.md 中所有 Todo 项标记为 `[x]` 或用户明确表示完成
---

## 整体流程

```
读取/创建 project.md 
    → 检索理解项目 
    → 🛑 停！检索 ≥3 文件？必须先更新 project.md
    → 创建 request.md 
    → 多轮澄清需求 
    → 🛑 停！用户确认了吗？
    → 生成 todolist.md 
    → 执行 Todo(刷新上下文 → 检索 → 🛑债务≥3?先沉淀 → 执行 → 更新状态)
    → 循环直到完成 
    → 归档任务
```

---

## 📢 动作输出规范

执行关键动作时必须输出提示,让用户知道 FlowMem 流程状态:

| 动作 | 输出格式 |
|------|----------|
| 流程启动 | 🚀 **FlowMem 已启动** - 触发原因: [信号] |
| 文件操作 | 📝 **[创建/读取/更新/归档]**: `.agentmem/[文件名]` |
| 上下文刷新 | 🔄 **刷新上下文** - 当前 Todo: [任务] |
| 需求澄清 | ❓ **需求澄清** (第 X 轮) - 待确认: [问题] |
| 知识沉淀 | 💡 **知识沉淀** - 补充到: [文件] |
| 任务归档 | ✅ **任务完成** - 已归档: [文件列表] |

🚨 **输出即执行约束：**
- 输出「💡 知识沉淀」后，**下一个工具调用必须是更新文件**
- 输出「📝 创建」后，**下一个工具调用必须是写入文件**
- 禁止：输出提示后跳过，继续做其他事情

**示例:** `🚀 **FlowMem 已启动** - 触发原因: 涉及 5+ 文件修改`
---

## 7 条关键规则

### 规则 1: 上下文管理

**前提:** 执行复杂任务前必须先创建/读取 `project.md`。初始版本可以很简单,随任务逐步完善。

**刷新顺序:** `todolist.md` → `request.md` → `project.md`

**何时刷新:**
- ✅ 必须:跨模块、连续3+ Todo、遇错、用户变更、复杂依赖
- ✅ 建议:跨阶段、新代码区域、上下文超50k tokens
- ⚠️ 可选:同模块连续任务、批量类似操作、纯文档编写

**刷新流程:**
1. 读取当前 Todo - 明确当前要做什么
2. 读取需求目标 - 确保理解用户真实意图
3. 读取项目背景 - 补充必要的项目认知
4. 如信息不足,检索代码/文档
5. 立即补充新知识到文档

> 💡 **判断原则**: 任务独立性越强 = 越需要刷新；任务连续性越强 = 越可批量执行

### 规则 2: 需求先澄清,确认再执行

**流程:**
1. 创建 `request.md`,记录原始需求
2. AI 提出澄清问题(至少 1 轮,**豁免:** 用户明确要求「直接执行」「跳过确认」时可省略)
3. **用户回答后立即更新 `request.md`**
4. 循环直到需求明确
5. **用户确认后先更新状态为「已确认」,再生成 `todolist.md`**

🚨 **强制确认信号:**
- 必须等用户明确回复「确认」「开始」「可以执行」等肯定词才能开始执行
- 即使需求看起来很清晰,也必须至少进行 1 轮澄清确认
- 禁止假设"需求已明确"而跳过确认环节

⚠️ 禁止用户回复后直接开始执行,必须先更新文档。

**需求确认后自检:** 需求描述完整?技术方案可行?验收标准明确?

**执行中需求变更:** 用户在执行 Todo 过程中提出新需求/变更 → 立即更新 `request.md` → 评估影响 → 更新 `todolist.md`

### 规则 3: 🚨 智能执行节奏

**🔴 默认单步原则:** 除非明确符合批量条件,否则一律单步执行。

**执行策略:**
- **单步模式**(默认):执行 1 个 Todo → 更新 todolist.md → 刷新上下文 → 下一个
- **批量模式**(需显式判断):批量不超过 3 个同质化任务,且必须同时满足以下全部条件:
  - ✅ 同一模块内的连续任务
  - ✅ 任务之间无依赖关系
  - ✅ 非首次接触的代码区域
  - ✅ 低风险操作(非数据库/权限/配置变更)

**❌ 禁止批量(任一条件即禁止):**
- 数据库迁移/权限变更等高风险操作
- 任务有强依赖(前一个失败影响后续)
- 跨多个模块或系统
- 首次接触的新功能或代码区域
- 涉及新 API/新框架的使用

> 💡 **判断口诀**: 拿不准就单步,宁慢勿错

### 规则 4: 存储而非填充
长篇内容存入文件,上下文中只保留路径引用。

### 规则 5: 知识沉淀（债务机制）

**🔴 沉淀债务计数：**
- 每 read_file 一次 = 债务 +1
- 更新 project.md/notes.md = 债务清零
- 🚨 **债务 ≥ 3 时：禁止继续检索或执行，必须先结清债务**

**❗ Blocking 指令（强制）:**
> 债务 ≥ 3 时，可以完成当前检索，但**沉淀前禁止任何非更新操作**（如创建 request.md、编写代码等）。
> 下一个工具调用必须是 `write_file` 或 `replace_file_content` 更新 project.md/notes.md。

**沉淀去哪：**
- 架构/API/模块知识 → `project.md`
- 需求边界/确认细节 → `request.md`
- 临时发现/调试信息 → `notes.md`

**防膨胀：**
- `project.md` 超过 300 行 → 迁移详细内容到 `docs/`
- 单模块超过 50 行 → 拆分为独立文档 `docs/modules/xxx.md`

**❌ 典型违规流程（禁止）:**
```
read_file(a.ts) → read_file(b.ts) → read_file(c.ts) → 创建 request.md
```

**❌ 输出但未执行（禁止）:**
```
read_file x3 → 输出"💡 知识沉淀" → 跳过更新 → 创建 request.md
```

**✅ 正确流程:**
```
read_file(a.ts) → read_file(b.ts) → read_file(c.ts) 
→ 输出"💡 知识沉淀"
→ write_file(更新 project.md，记录 a/b/c 的关键发现)
→ 创建 request.md
```

### 规则 6: 上下文优化（Manus 4 原则^[来源: Manus AI 最佳实践]）

| 原则 | 实践 |
|------|------|
| 仅追加上下文 | 新信息始终追加,不修改历史 |
| 稳定前缀 | 系统指令 → 项目背景 → 动态内容 |
| 避免过拟合 | 稍微改变措辞,重新校准 |
| 可逆压缩 | 存储路径而非删除 |

### 规则 7: 任务完成后清理

**归档到 `history/YYYYMMDD_[文件名]`:**
- 任务周期文件: `request.md` + `request_detail/`、`todolist.md` + `task_logs/`、`notes.md`
- 清理所有任务相关的临时文件和副作用文件

**保留:** `project.md` 和 `docs/`

---
## ✅ AI 自检清单

**触发时机:** 每次执行 Todo、用户回复、准备归档时

**🔴 知识沉淀（最重要）:**
- [ ] 检索 ≥ 3 文件但沉淀 = 0？**停止，先补充文档**

**上下文与文档:**
- [ ] 需要刷新？（跨模块/连续3+ Todo/遇错/新代码区域）
- [ ] 用户回复 → 已更新 request.md?
- [ ] 完成 Todo → 已更新 todolist.md?

**任务与执行:**
- [ ] 在任务中？用户回复是任务的一部分，别退出
- [ ] 遵循单步执行、立即更新原则？

---

## 反模式对照

| ❌ 不要 | ✅ 而是 |
|---------|---------|
| 使用 AI 内置记忆 | 创建 `.agentmem/` 文件 |
| 只说一次目标 | 每次决策前重读 todolist.md |
| 静默重试错误 | 记录到 task_logs,添加解决任务 |
| 内容塞进上下文 | 存文件,引用路径 |
| 不澄清直接执行 | 通过 request.md 多轮确认 |
| 检索后延后补充 | 检索后立即补充 |
| 高风险任务批量执行 | 单步执行,及时更新 |
| todolist 写详细日志 | 存入 task_logs/,只放链接 |
| 需求变更不记录 | 立即更新 request.md |
| 输出提示后跳过执行 | 输出后立即执行对应操作 |

---

## 参考资源

详细模板、示例和工具见 `.flowmem/` 目录
