{
  "version": "2.0.0",
  "projectRoot": "{{projectRoot}}",
  "config": {{flowMemConfig}},
  "rules": [
    {
      "name": "FlowMem Integration",
      "patterns": ["**/*"],
      "action": "allow"
    }
  ]
}
